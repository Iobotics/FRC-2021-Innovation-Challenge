import { StyleSheet } from 'react-native';
import { backColor } from '../../css/colors';

export default StyleSheet.create({
    arrow: {
        color: backColor,
        margin: 'auto',
        fontSize: 50
    }
});